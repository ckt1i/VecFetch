# 论文目录最小改动版重构建议

日期：2026-05-13

## 调整原则

这一版不追求把所有章节都改成完整三级目录，而是采用“二级目录为主、必要处补三级目录”的方式。相比上一版，三级标题只用于两类位置：

1. 某个二级节内部本来就包含多个并列技术点，例如 RaBitQ、BBC。
2. 某个二级节过长，且正文已有自然分段，例如实验设置、主实验数据集结果、机制消融。

因此，本版的改动目标是最小化：

- 第 1 章基本不改。
- 第 2 章保留现有主体顺序，但将存储后端与 I/O 调度合并为一个支撑性二级节。
- 第 3 章只合并相邻的数据布局节，避免模型章过碎。
- 第 4 章把“候选生成、误差界判定、渐进筛查”收束到同一个二级节下。
- 第 5 章把实验设置和实验分析按自然块组织，避免每个结果都成为孤立二级节。

## 建议新版目录

### 第 1 章 绪论

1.1 研究背景与意义

1.2 研究问题与挑战

1.3 本文主要研究内容与贡献

1.4 论文组织结构

说明：第 1 章当前结构已经足够清楚，不建议强行拆三级目录。绪论的重点是顺畅引出问题，不需要在目录层面暴露太多细节。

### 第 2 章 背景与相关工作

2.1 候选生成与高召回重排路径

2.2 向量量化估计与可控误差界

2.2.1 量化距离估计与重排精化

2.2.2 RaBitQ 的可控误差界与边界候选处理

说明：BBC 等 bounded quantization 相关工作不建议单独出现在目录标题中，可在 2.2.2 正文中作为“误差界如何服务边界候选重排”的相关工作介绍。这样目录重点落在本文真正需要的接口：RaBitQ 提供可控误差界，本文把该误差界进一步用于原始记录访问判定。

2.3 固定候选预算下的原始记录访问问题

2.4 原始记录访问的存储与调度基础

2.4.1 向量存储后端与访问粒度

2.4.2 异步 I/O 与读取调度

2.4.3 跨阶段检索流水线

2.5 本文工作定位

说明：建议把原 2.4 和原 2.5 打包成新版 2.4。理由是二者都服务于“原始记录访问如何被存储后端和读取执行层支撑”，并不是论文主贡献本身。跨阶段检索流水线可以作为 2.4.3 的短小补充，用于说明本文调度粒度只发生在单次 ANNS 查询内部；具体方法名不进入目录标题，避免偏离主线。原 `本文工作定位` 顺延为 2.5，作为相关工作章的收束。

### 第 3 章 面向候选访问判定的数据组织

3.1 记录对象、返回语义与访问粒度

3.2 量化簇表示与常驻簇视图

3.3 统一原始记录布局与逻辑读取范围

3.3.1 统一原始记录布局

3.3.2 地址列与逻辑读取范围

3.4 查询前预加载与在线访问边界

3.5 本章小结

说明：第 3 章当前逻辑本身比较顺，只是“统一原始记录布局”和“地址列与逻辑读取范围”关系很近，可以合并成一个二级节，内部保留两个三级节。这样既减少二级目录碎片，也不需要大改正文。

### 第 4 章 VecFetch 访问判定与查询调度

4.1 查询流水线总览

4.2 候选估计与状态判定

4.2.1 候选生成与量化距离估计

4.2.2 校准误差界与候选状态定义

4.2.3 渐进式候选筛查

4.3 候选状态到读取动作的映射

4.4 异步提交、精确重排与最终补读

4.5 质量控制与退化策略

4.6 本章小结

说明：第 4 章是当前最需要收束的章节。原来的“候选生成与量化距离估计”“校准误差界与候选状态判定”“渐进式候选筛查”都服务于同一个问题：如何从候选估计得到状态判定。因此建议统一放入 4.2。后面的读取动作、异步执行、质量控制仍作为二级节保留，形成清晰链条：流水线 -> 判定 -> 动作 -> 执行 -> 质量控制。

### 第 5 章 实验评估与分析

5.1 实验设置与评价口径

5.1.1 实验问题与质量对齐原则

5.1.2 数据集、系统与实验环境

5.1.3 评价指标与统计口径

5.2 主实验：端到端检索性能

5.2.1 COCO100K

5.2.2 MS MARCO Passage

5.2.3 Deep1M_synth

5.3 存储后端参考实验

5.4 机制消融分析

5.4.1 候选访问判定消融

5.4.2 异步提交与调度消融

5.5 Top20 返回规模实验

5.6 本章小结

说明：第 5 章建议把“实验问题、指标”和“数据集、系统、环境”合并为 5.1，避免实验设置被拆得过细。主实验仍保留为一个二级节，三个数据集作为三级节，这比把每个数据集都放成二级节更连贯。候选访问判定消融和异步调度消融都属于机制分析，因此合并到 5.4。

### 结论与展望

保持现有结论部分，不建议新增正文章节。

## 与当前目录的最小迁移关系

### 基本保留

- 第 1 章四个二级节全部保留。
- 第 2 章主体顺序保留，但将原 2.4 与原 2.5 合并为新版 2.4。
- 第 3 章主体顺序保留。
- 第 4 章主体顺序保留。
- 第 5 章主体顺序保留。

### 建议合并或降级

- 当前 3.3 `统一原始记录布局` 与 3.4 `地址列与逻辑读取范围` 合并为新版 3.3，原二者降为 3.3.1 和 3.3.2。
- 当前 4.2、4.3、4.4 合并为新版 4.2，原二者分别降为 4.2.1、4.2.2、4.2.3。
- 当前 5.1 与 5.2 合并为新版 5.1，内部拆成 5.1.1 到 5.1.3。
- 当前 5.5 与 5.6 合并为新版 5.4，分别作为 5.4.1 和 5.4.2。
- 当前 2.4 `向量存储后端与访问粒度` 和当前 2.5 `异步 I/O 与读取调度` 合并为新版 2.4，分别降为 2.4.1 和 2.4.2；跨阶段流水线只作为 2.4.3 的补充视角。

### 建议只新增三级标题

- 第 2 章 2.2 内只保留一个 RaBitQ 误差界相关三级标题，BBC 在正文中介绍，不单独进入目录。
- 第 2 章 2.4 内新增三级标题，用于把存储后端、单次查询内部 I/O 调度和跨阶段流水线放在同一支撑性二级节下。
- 第 5 章 5.2 内新增三个数据集三级标题，提升主实验结果的可读性。

## 这版目录的叙述逻辑

这版目录的主线是：

1. 绪论提出候选访问判定问题。
2. 相关工作说明：候选生成和量化误差界已有基础，但从误差界到原始记录访问动作的连接仍是本文关注点。
3. 数据组织章定义可被访问判定使用的对象和读取范围。
4. 方法章解释 VecFetch 如何判定候选状态，并把状态映射到读取动作和异步执行。
5. 实验章先回答端到端是否有效，再分析机制来源和返回规模变化。

相比上一版，目录层级更克制，保留了当前论文的大部分二级结构；相比当前主稿，它又把最碎的部分收束成了更清楚的逻辑单元。
